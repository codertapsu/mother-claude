{
  "name": "mother-claude-sidecar",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Optional Path A bridge: drives an owned session via the Claude Agent SDK with canUseTool + ask_user, routing approvals to the Mother Claude server.",
  "main": "dist/agent-bridge.js",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.186"
  },
  "devDependencies": {
    "@types/node": "26.0.0",
    "typescript": "6.0.3"
  }
}
